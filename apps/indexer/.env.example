# Celo Sepolia RPC (default forno endpoint works; use a private RPC for production throughput).
PONDER_RPC_URL_11142220=https://forno.celo-sepolia.celo-testnet.org

# Deployed contract addresses + the block each was deployed at (fill after deploy).
TRWI_ADDRESS=
TRWI_START_BLOCK=
STAKING_ADDRESS=
STAKING_START_BLOCK=

# Same Postgres the web app uses (Ponder writes the indexed tables here).
DATABASE_URL=
