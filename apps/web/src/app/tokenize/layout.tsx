import type { ReactNode } from "react";

export const metadata = {
  title: "Tokenize impact",
  description: "Describe the real-world impact you delivered; the Impact Value updates live, then a validator reviews it.",
};

export default function TokenizeLayout({ children }: { children: ReactNode }) {
  return children;
}
